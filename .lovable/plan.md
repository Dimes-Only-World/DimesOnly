

## Plan: Reorder Carousels on Home Page

Swap `LatestDimesCarousel` and `ImageCarousel` in `src/pages/NewIndex.tsx` so that `ImageCarousel` (Top 20 Ranked) comes first, followed by `LatestDimesCarousel` (Latest 20 Dimes).

### File Changed
- `src/pages/NewIndex.tsx`: Swap lines for `<ImageCarousel />` and `<LatestDimesCarousel />`.

